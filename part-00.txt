<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Lead Tracker | Talk Dygital</title>
  <style>
    :root {
      --bg: #071116;
      --panel: #101d24;
      --panel-2: #15252d;
      --line: #28404a;
      --text: #edf7f5;
      --muted: #9db2b7;
      --accent: #1DB89B;
      --accent-2: #62e2ca;
      --danger: #ff6b6b;
      --warn: #f8bd4e;
      --blue: #68a8ff;
      --radius: 8px;
      --shadow: 0 18px 60px rgba(0, 0, 0, .28);
      color-scheme: dark;
      font-family: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
    }

    * { box-sizing: border-box; }

    body {
      margin: 0;
      min-height: 100vh;
      background: var(--bg);
      color: var(--text);
    }

    button, input, select, textarea {
      font: inherit;
    }

    button {
      border: 0;
      cursor: pointer;
      touch-action: manipulation;
    }

    .app {
      min-height: 100vh;
      display: grid;
      grid-template-columns: 280px minmax(0, 1fr);
    }

    .sidebar {
      padding: 28px 20px;
      background: #09161c;
      border-right: 1px solid var(--line);
      position: sticky;
      top: 0;
      height: 100vh;
      overflow-y: auto;
    }

    .brand {
      display: flex;
      gap: 12px;
      align-items: center;
      margin-bottom: 28px;
    }

    .brand-mark {
      width: 44px;
      height: 44px;
      display: grid;
      place-items: center;
      border-radius: 8px;
      background: var(--accent);
      color: #05251f;
      font-weight: 900;
      letter-spacing: 0;
    }

    .brand h1 {
      font-size: 19px;
      margin: 0;
      letter-spacing: 0;
      line-height: 1.1;
    }

    .brand p {
      margin: 3px 0 0;
      color: var(--muted);
      font-size: 13px;
    }

    .nav {
      display: grid;
      gap: 8px;
    }

    .nav button,
    .quick-button,
    .primary,
    .secondary,
    .icon-button,
    .chip,
    .mini-button {
      min-height: 46px;
      border-radius: var(--radius);
      color: var(--text);
      background: transparent;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      gap: 9px;
      transition: .16s ease;
    }

    .nav button {
      justify-content: flex-start;
      padding: 0 12px;
      color: var(--muted);
    }

    .nav button.active,
    .nav button:hover {
      color: var(--text);
      background: var(--panel-2);
    }

    .nav .count {
      margin-left: auto;
      min-width: 28px;
      height: 24px;
      border-radius: 999px;
      display: grid;
      place-items: center;
      background: rgba(29, 184, 155, .15);
      color: var(--accent-2);
      font-size: 12px;
      font-weight: 800;
    }

    .sidebar-footer {
      margin-top: 26px;
      padding-top: 20px;
      border-top: 1px solid var(--line);
      display: grid;
      gap: 10px;
    }

    .main {
      min-width: 0;
      padding: 28px;
    }

    .topbar {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 18px;
      margin-bottom: 22px;
    }

    .topbar h2 {
      margin: 0;
      font-size: 28px;
      letter-spacing: 0;
    }

    .topbar p {
      margin: 5px 0 0;
      color: var(--muted);
    }

    .primary {
      background: var(--accent);
      color: #04231e;
      font-weight: 900;
      padding: 0 16px;
    }

    .primary:hover { background: var(--accent-2); }

    .secondary {
      background: var(--panel-2);
      color: var(--text);
      border: 1px solid var(--line);
      padding: 0 14px;
    }

    .secondary:hover { border-color: var(--accent); }

    .quick-button {
      background: rgba(29, 184, 155, .12);
      color: var(--accent-2);
      border: 1px solid rgba(29, 184, 155, .25);
      padding: 0 12px;
      font-weight: 800;
    }

    .grid {
      display: grid;
      gap: 16px;
    }

    .stats {
      grid-template-columns: repeat(3, minmax(0, 1fr));
      margin-bottom: 18px;
    }

    .stat,
    .panel,
    .lead-card,
    .person-card,
    .client-card {
      background: var(--panel);
      border: 1px solid var(--line);
      border-radius: var(--radius);
      box-shadow: var(--shadow);
    }

    .stat {
      padding: 16px;
      min-height: 98px;
    }

    .stat span {
      display: block;
      color: var(--muted);
      font-size: 13px;
      margin-bottom: 10px;
    }

    .stat strong {
      font-size: 32px;
      line-height: 1;
    }

    .due-strip {
      display: none;
      margin-bottom: 18px;
      border: 1px solid rgba(248, 189, 78, .45);
      background: rgba(248, 189, 78, .09);
      border-radius: var(--radius);
      overflow: hidden;
    }

    .due-strip.show { display: block; }

    .auth-panel {
      display: none;
      margin-bottom: 18px;
      padding: 14px;
      background: var(--panel);
      border: 1px solid var(--line);
      border-radius: var(--radius);
      box-shadow: var(--shadow);
    }

    .auth-panel.show { display: block; }

    .auth-grid {
      display: grid;
      grid-template-columns: minmax(180px, 1fr) minmax(160px, 1fr) auto auto auto;
      gap: 10px;
      align-items: center;
    }

    .auth-status {
      color: var(--muted);
      font-size: 13px;
      margin-top: 10px;
    }

    .due-head {
      display: flex;
      justify-content: space-between;
      gap: 14px;
      padding: 14px 16px;
      border-bottom: 1px solid rgba(248, 189, 78, .24);
      color: #ffe1a1;
      font-weight: 900;
    }

    .due-list {
      display: grid;
      gap: 1px;
      background: rgba(248, 189, 78, .12);
    }

    .due-item {
      display: grid;
      grid-template-columns: minmax(140px, 1.2fr) minmax(110px, .7fr) minmax(120px, .7fr) auto;
      gap: 10px;
      align-items: center;
      min-height: 58px;
      padding: 10px 14px;
      background: #101d24;
    }

    .toolbar {
      display: grid;
      grid-template-columns: minmax(170px, 1fr) repeat(4, minmax(140px, 180px));
      gap: 10px;
      margin-bottom: 16px;
    }

    .control,
    .field input,
    .field select,
    .field textarea {
      width: 100%;
      min-height: 48px;
      border-radius: var(--radius);
      border: 1px solid var(--line);
      background: var(--panel-2);
      color: var(--text);
      padding: 0 12px;
      outline: none;
    }

    .control:focus,
    .field input:focus,
    .field select:focus,
    .field textarea:focus {
      border-color: var(--accent);
      box-shadow: 0 0 0 3px rgba(29, 184, 155, .16);
    }

    .field textarea {
      min-height: 96px;
      resize: vertical;
      padding-top: 12px;
    }

    .lead-list,
    .client-list {
      display: grid;
      gap: 10px;
    }

    .lead-card {
      display: grid;
      grid-template-columns: minmax(180px, 1.3fr) minmax(130px, .8fr) minmax(150px, .8fr) minmax(130px, .8fr) minmax(130px, .8fr) auto;
      gap: 12px;
      align-items: center;
      padding: 14px;
    }

    .person-card {
      padding: 14px;
    }

    .person-top {
      display: grid;
      grid-template-columns: minmax(190px, 1fr) auto;
      gap: 12px;
      align-items: start;
      margin-bottom: 10px;
    }

    .person-summary {
      display: flex;
      flex-wrap: wrap;
      justify-content: flex-end;
      gap: 8px;
    }

    .session-stack {
      display: grid;
      gap: 8px;
    }

    .session-line {
      display: grid;
      grid-template-columns: minmax(190px, 1fr) minmax(120px, .5fr) minmax(120px, .5fr) minmax(160px, .8fr) auto;
      gap: 10px;
      align-items: center;
      padding: 10px 0;
      border-top: 1px solid var(--line);
    }

    .session-line:first-child {
      border-top-color: rgba(255,255,255,.04);
    }

    .session-main {
      min-width: 0;
    }

    .session-main b,
    .session-line b {
      display: block;
      overflow: hidden;
      text-overflow: ellipsis;
      white-space: nowrap;
    }

    .client-card {
      display: grid;
      grid-template-columns: minmax(180px, 1.2fr) minmax(130px, .8fr) repeat(4, minmax(100px, .65fr)) auto;
      gap: 12px;
      align-items: center;
      padding: 14px;
    }

    .name-button {
      border: 0;
      padding: 0;
      min-height: 0;
      background: transparent;
      color: var(--text);
      font-weight: 900;
      text-align: left;
      font-size: 16px;
      justify-content: flex-start;
    }

    .name-button:hover { color: var(--accent-2); }

    .small {
      color: var(--muted);
      font-size: 13px;
      margin-top: 4px;
    }

    .pill {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      min-height: 32px;
      padding: 5px 10px;
      border-radius: 999px;
      background: rgba(157, 178, 183, .12);
      color: var(--text);
      font-size: 13px;
      font-weight: 800;
      width: fit-content;
    }

    .pill.high { background: rgba(255, 107, 107, .15); color: #ffb4b